package Modul02.Opgaver;

public class A1 {

    public static void main(String[] args) {

        // create vairbales radius and area.
        double radius;
        double area;

        // Radius and area get a specfik varibal.
        radius = 1;
        area = radius * radius * Math.PI;
        // Print result.
        System.out.println("This is the area when the radius is one " + area);

        // Radius and area get a specfik varibal.
        radius = 2;
        area = radius * radius * Math.PI;
        // Print result.
        System.out.println("This is the area when the radius is two " + area);

        // Radius and area get a specfik varibal.
        radius = 3;
        area = radius * radius * Math.PI;
        // Print result.
        System.out.println("This is the area when the radius is three " + area);

        // Radius and area get a specfik varibal.
        radius = 4;
        area = radius * radius * Math.PI;
        // Print result.
        System.out.println("This is the area when the radius is four " + area);

        // Radius and area get a specfik varibal.
        radius = 5;
        area = radius * radius * Math.PI;
        // Print result.
        System.out.println("This is the area when the radius is five " + area);

    }
}
